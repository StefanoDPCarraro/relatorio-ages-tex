```bash
chmod +777 ./clean.sh ./compile.sh ./compile-fast.sh
```

```bash
./clean.sh
./compile.sh
./compile-fast.sh
./compile-compressed.sh
```

O último comando também cria `main-comprimido.pdf`, uma versão otimizada para
compartilhamento, preservando o arquivo `main.pdf` original.
